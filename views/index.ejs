<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BlogMaker</title>
</head>
<body>
    <%- include("partials/header.ejs") %>

    <a class="nav-link" href="/create">Submit a blog entry</a>
    <% if (locals.listOfEntries) { %>
        <h3><% listOfEntries.forEach(element => { %>
         <%= element %>
        <% }) %></h3>
     
    <% } %>


    <%- include("partials/footer.ejs") %>
</body>
</html>

